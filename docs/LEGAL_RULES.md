# Regras jurídicas como dados

Registre cada regra com `legal_basis`, `source`, `valid_from`, `valid_until`, `consulted_at` e, quando possível, `sha256`. O repositório traz apenas um registro mínimo de fontes oficiais; a aplicação concreta depende do título, categoria, período e documentos do caso.
