# Instalação

Requer Python 3.12+. Crie um ambiente virtual e instale `pip install -e ".[dev]"`. Para exportação DOCX/PDF, instale `pip install -e ".[documents]"`.

Depois rode `calculadora-trabalhista doctor`, `calculadora-trabalhista validate` e `pytest`.
